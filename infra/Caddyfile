{$APP_DOMAIN} {
    encode gzip
    reverse_proxy api:8000

    @health path /healthz /readyz
    handle @health {
        reverse_proxy api:8000
    }

    log {
        output stdout
        format console
    }
}
